### 테스트의 필요성
1. 버그를 조기에 발견

- 개발 초기에 발견할수록 수정 비용이 낮음

- 운영 중 장애 → 신뢰도·비용·리스크 급증

2. 리팩토링의 안전망

- 코드 개선 시 기존 기능이 깨졌는지 바로 확인 가능

- 테스트 없는 리팩토링 = 감으로 하는 작업

3. 문서 역할

- “이 메서드는 이렇게 동작해야 한다”를 코드로 설명

- 신규 입사자나 협업 시 이해도 상승

4. 설계 품질 향상

- 테스트가 어렵다면 → 설계가 복잡하거나 책임이 과도할 가능성

- 테스트 가능한 코드 = 응집도 높고 결합도 낮은 코드

## 좋은 테스트를 만들기 위해서 고려해야 하는 것들

> **Red - Green - Refactor 사이클**
>
> 🔴 Red: 실패하는 테스트 작성
> 
> 🟢 Green: 빠르게 통과시키기
>
> 🔵 Refactor: 중복 제거, 구조 개선


### 1. 테스트는 작고 단일한 책임을 가져야 한다
- 하나의 테스트는 **하나의 행위(Behavior)** 만 검증해야 한다.
- 여러 조건이나 시나리오를 한 테스트에 넣으면 실패 원인을 파악하기 어렵다.

**좋은 예**
- “비밀번호가 8자 미만일 때 회원가입이 실패한다”를 검증하는 테스트
- “비밀번호에 특수문자가 없을 때 회원가입이 실패한다”를 검증하는 테스트

**안 좋은 예**
- 하나의 테스트에서 비밀번호 길이, 특수문자 포함 여부, 대소문자 규칙까지 모두 검증하는 경우  
  → 어떤 조건 때문에 실패했는지 바로 알 수 없음

---

### 2. 테스트는 구현이 아니라 행위를 검증해야 한다
- 내부 구현 방식이 아니라 **외부에서 관찰 가능한 결과**를 검증해야 한다.
- 구현 변경으로 테스트가 깨진다면 좋은 테스트가 아니다.

**좋은 예**
- 주문을 생성했을 때 주문 상태가 “완료”로 변경되는지를 검증
- 잘못된 입력이 들어왔을 때 예외가 발생하는지를 검증

```java
@Test
void registerUser_returns_success_result() {
    UserRepository repository = mock(UserRepository.class);
    UserService service = new UserService(repository);

    User user = new User("kim");

    RegisterResult result = service.register(user);

    assertTrue(result.isSuccess());
}

```

**안 좋은 예**
- 특정 메서드가 몇 번 호출됐는지, 내부 컬렉션에 어떤 값이 들어갔는지를 직접 검증  
  → 내부 구조 변경 시 테스트가 의미 없이 깨짐

```java
@Test
void registerUser_calls_repository_save() {
    UserRepository repository = mock(UserRepository.class);
    UserService service = new UserService(repository);

    User user = new User("kim");

    service.register(user);

    // 내부 구현에 강하게 의존
    verify(repository, times(1)).save(user);
}

```

---

### 3. 테스트는 읽는 사람에게 의도를 명확히 드러내야 한다
- 테스트 코드는 **실행 가능한 문서**다.
- 테스트 이름과 구조만 봐도 무엇을 보장하는지 알 수 있어야 한다.

**좋은 예**
- 테스트 이름만 보고 “로그인에 실패하는 조건”을 바로 이해할 수 있는 경우

```java
@DisplayName("틀린 비밀번호로 로그인을 시도하면 401 에러를 반환한다.")
@Test
void login_fail_when_password_is_wrong() {
    User user = new User("kim", "wrong-password");
    AuthService authService = new AuthService();

    assertThrows(AuthException.class, () -> authService.login(user));
}

```

**안 좋은 예**
- `test1`, `caseA`, `successTest`처럼 의미를 알 수 없는 이름  
  → 코드를 직접 읽지 않으면 테스트 목적을 알 수 없음

---

### 4. 테스트는 서로 독립적이어야 한다
- 테스트 실행 순서에 의존하면 안 된다.
- 하나의 테스트 실패가 다른 테스트 결과에 영향을 주면 안 된다.

**좋은 예**
- 각 테스트가 실행될 때마다 동일한 초기 상태에서 시작
- 어떤 테스트를 단독으로 실행해도 항상 같은 결과가 나옴

**안 좋은 예**
- 앞선 테스트에서 생성한 데이터를 다음 테스트가 암묵적으로 사용하는 경우  
  → 실행 순서가 바뀌면 실패

---

### 5. 테스트는 빠르고 반복 가능해야 한다
- 테스트는 자주 실행될 수 있어야 의미가 있다.
- 느린 테스트는 결국 실행되지 않게 된다.

**좋은 예**
- 메모리 내 객체와 가짜 의존성을 사용해 수 밀리초 안에 끝나는 테스트

**안 좋은 예**
- 실제 데이터베이스, 네트워크 호출, 외부 API에 의존하는 테스트  
  → 느리고 환경에 따라 실패

---

### 6. 외부 의존성은 통제되어야 한다
- 단위 테스트는 외부 시스템의 상태에 영향을 받지 않아야 한다.
- 외부 의존성은 테스트 대상의 경계 밖으로 밀어낸다.

**좋은 예**
- 결제 시스템, 이메일 발송, 현재 시간 등을 테스트에서 고정된 값으로 대체

**안 좋은 예**
- 테스트 실행 시 실제 결제 요청이나 이메일 발송이 발생하는 경우  
  → 테스트가 불안정하고 위험함

---

### 7. 실패했을 때 원인이 명확해야 한다
- 테스트 하나가 실패하면 “무엇이 깨졌는지” 바로 알 수 있어야 한다.

**좋은 예**
- 실패 메시지를 통해 어떤 조건이 만족되지 않았는지 바로 알 수 있음

**안 좋은 예**
- 여러 검증이 한 테스트에 섞여 있어 어디서 실패했는지 추적해야 하는 경우


### JUnit 에서 자주 사용되는 개념

1. 기본 Assertion
```java
assertEquals(expected, actual);
assertNotEquals(a, b);
assertTrue(condition);
assertFalse(condition);
assertNull(value);
assertNotNull(value);
```

2. 예외 테스트

```java
assertThrows(IllegalArgumentException.class, () -> {
    service.find(null);
});
```

3. 모든 검증 실행 (assertAll)
```java
assertAll(
    () -> assertEquals("kim", user.getName()),
    () -> assertEquals(20, user.getAge())
);
```

4. Parameterized Test
```java
@ParameterizedTest
@ValueSource(ints = {1, 2, 3})
void isPositive(int number) {
    assertTrue(number > 0);
}

@CsvSource({
    "2,3,5",
    "1,4,5"
})
void addTest(int a, int b, int result) {
    assertEquals(result, calculator.add(a, b));
}
```